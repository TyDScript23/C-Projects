#include<iostream>
#include<iomanip>
#include<cstdlib>

/*using namespace std;

int main(int argc, char* argv[])	//argc - number of arguments and *argv[] - array of pointers to each of rest of arguements
{
	int mid, fin;
	float avg;
	cout.setf(ios::showpoint | ios::fixed);
	if (argc >= 3)
		// make sure user entered 2 grades when running the program 
	{
		mid = atoi(argv[1]);
		// convert 1st argument (after program name) to integer 
		fin = atoi(argv[2]);	// convert 2nd 
		avg = (mid + fin) / 2.0;
		cout << setprecision(2) << avg << endl;
	}
	return 0;
}*/
