/* Tyler Davis
Exercise 1-1
This program will convert fahrenheit to celsius
*/
/*#include<iostream>        // for the input/output streams - cin, cout, and endl
#include<iomanip>        // for the stream manipulators - fixed, showpoint, setprecision
using namespace std;    // use std::cin and std::cout directly

int main()
{
    int fah;            // variables
    float cel;
    // set properties on cout to follow setprecision
    cout.setf(ios::fixed | ios::showpoint);
    cout << "Enter fahrenheit temperature:  ";   // stream insertion
    cin >> fah;                    // stream extraction
    cel = (fah - 32) * 5 / 9.0;    // conversion to celsius
    cout << "Temperature in Celsius is " << setprecision(2)
        << cel << endl;      // 3 decimal places before variable
    return 0;
}*/