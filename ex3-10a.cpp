#include <iostream>

/*using namespace std;

int main()
{
	char inputs[3][10];
	cout << "Input your three words here: " << endl;
	cin >> inputs[0] >> inputs[1] >> inputs[2];
	int i;

	for (i = 0; i <= strlen(inputs[i]); i++)
		if (inputs[i][strlen(inputs[i]) - 2] == 'e' && inputs[i][strlen(inputs[i]) - 1] == 's')
			cout << inputs[i] << endl;

	return 0;
}*/