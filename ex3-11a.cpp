#include <iostream>

/*using namespace std;

int main()
{
	char integers[4][50];
	int i, j;

	for (i = 0; i < 4; i++)
	{
		cout << "Enter integer number " << i + 1 << " here: " << endl;
		cin >> integers[i];
	}

	cout << endl;

	bool digit = true;
	int sum = 0;

	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < strlen(integers[i]); j++)
		{
			if (isdigit(integers[i][j]))
			{
				sum += integers[i][j] - '0';
			}
			else
			{
				digit = false;
			}
		}
		cout << sum << endl;
		sum = 0;
	}
	
	return 0;
}*/